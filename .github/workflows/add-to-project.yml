name: Add issues to project

on:
  issues:
    types:
      - opened

jobs:
  add-to-project:
    name: Add issue to project
    runs-on: Linux
    steps:
        # This action doesn't exist in enterprise, manually added to cFS org for now
      - uses: cFS/add-to-project@v1.0.2
        with:
          # You can target a project in a different organization
          # to the issue
          project-url: https://github.com/orgs/cFS/projects/1
          github-token: ${{ secrets.ADD_TO_PROJECT_PAT }}