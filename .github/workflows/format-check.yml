name: Format Check

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    types:
      - opened
      - reopened
      - synchronize
  workflow_dispatch:

permissions:
  contents: read

jobs:
  format-check:
    name: Run format check
    uses: nasa/cFS/.github/workflows/format-check.yml@dev